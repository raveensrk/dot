#!/usr/bin/env bash

set -euo pipefail
IFS=$'\n\t'
# set -x

[[ -f ~/script/before_lg.py ]] && ~/script/before_lg.py

while read -u 10 -re line; do
	# gitui -d "${line/\~/$HOME}"
	lazygit -p "${line/\~/$HOME}"
	echo "${line/\~/$HOME}"
done 10<~/projects.txt
