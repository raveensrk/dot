#!/usr/bin/env bash

set -euo pipefail
IFS=$'\n\t'
# set -x

text="$*"
len="${#text}"
len=$((len + 4))

# printf "\t\t"
# {
# 	printjot -b - "$len" | rs -w200 -g 0
# 	printf "%s\n" "$text"
# 	jot -b - "$len" | rs -w200 -g 0
# } | lolcat

{
	print_centered - -
	print_centered "$text"
	print_centered - -
} | lolcat

exit 0
